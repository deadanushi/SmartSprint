# Backend Issues

This folder tracks pending backend tasks.

- Companies API: implement POST /api/companies for create
- Validation: unique name, required fields, normalized casing
- Response: return created company payload { id, name, industry }
